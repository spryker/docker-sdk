services:
    database:
        engine: postgres
    broker:
        engine: rabbitmq
    session:
        engine: redis
        version: '6.2'
    key_value_store:
        engine: redis
        version: '6.2'
    search:
        engine: elastic
        version: 7.6
    scheduler:
        engine: jenkins
    mail_catcher:
        engine: mailhog
