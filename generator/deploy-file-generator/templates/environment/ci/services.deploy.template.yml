services:
    database:
        engine: postgres
        root:
            username: 'root'
            password: 'secret'
    broker:
        engine: rabbitmq
        api:
            username: 'spryker'
            password: 'secret'
    session:
        engine: redis
    key_value_store:
        engine: redis
    search:
        engine: elastic
        version: 7.6
    scheduler:
        engine: jenkins
    mail_catcher:
        engine: mailhog
