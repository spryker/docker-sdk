      - logs:${SPRYKER_LOG_DIRECTORY}:rw
{% if docker.ssl.strict | default(false) %}
      - ./${DEPLOYMENT_PATH}/spryker_ca.crt:/certs/ca.crt:ro
      - ./${DEPLOYMENT_PATH}/ssl.crt:/certs/ssl.crt:ro
      - ./${DEPLOYMENT_PATH}/ssl.key:/certs/ssl.key:ro
{% endif %}
{% if _mountMode != 'baked' %}
{% if _mountMode == 'native' %}
      - ./:/data
{% else %}
      - ${SPRYKER_DOCKER_PREFIX}_${SPRYKER_DOCKER_TAG}_data_sync:/data:nocopy
{% endif %}
{% endif %}
