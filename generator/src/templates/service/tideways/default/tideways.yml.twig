  {{ serviceData.project_name ~ '_' ~ serviceName }}:
    container_name: {{ serviceData.project_name ~ '_' ~ serviceName }}
    build:
      context: {{ serviceData.deployment_path }}/images/common/services/tideways/
      dockerfile: Dockerfile
      args:
        TIDEWAYS_ENVIRONMENT_DEFAULT: "{{ serviceData.environment | default('production') }}"
    networks:
      - {{ internal_project_name }}_private
    labels:
      'spryker.app.name': {{ serviceData.project_name ~ '_' ~ serviceName }}
      'spryker.app.type': agents
      'spryker.project': ${SPRYKER_DOCKER_PREFIX}:${SPRYKER_DOCKER_TAG}
