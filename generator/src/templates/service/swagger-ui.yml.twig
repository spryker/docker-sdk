  {{ serviceName }}:
    image: swaggerapi/swagger-ui
    networks:
        - services
        - private
    environment:
        - SWAGGER_JSON=/var/specs/{{ serviceData['document']['name'] }}
    restart: always
    volumes:
        - $PWD/{{ serviceData['document']['path'] }}:/var/specs
