      APPLICATION_ENV = {{ project['environment'] | env_var }}
      SPRYKER_DEBUG_ENABLED = {{ (project['docker']['debug']['enabled'] ? 1 : 0) | env_var }}
      SPRYKER_ACTIVE_STORES = "{{ project['regions'][regionName]['stores'] | keys | join(',') }}"
      SPRYKER_STORE_SPECIFIC = {{ project['_storeSpecific'] | env_var }}
