{% extends 'jenkins-job.default.xml.twig' %}

{% block setup %}{% endblock setup %}
{#todo: need to deep merge#}
{% block command %}<![CDATA[
{% if job.store is not empty and job.region is empty %}APPLICATION_STORE="{{ job.store }}" {%endif%}{% if job.region is not empty %}SPRYKER_CURRENT_REGION="{{ job.region }}" {%endif%}COMMAND="{{ job.command | raw }}{% if job.store is not empty and job.region is not empty %} --store={{ job.store }}{%endif%}" bash /usr/bin/spryker.sh
PROJECT_NAME_PREFIX="{{ project_name ~ '_' | default("") }}" APPLICATION_STORE="{{ job.store }}" COMMAND="{{ job.command | raw }}" bash /usr/bin/spryker.sh
]]>{% endblock command %}
