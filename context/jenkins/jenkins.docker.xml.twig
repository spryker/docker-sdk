{% extends 'jenkins-job.default.xml.twig' %}

{% block setup %}{% endblock setup %}

{% block command %}<![CDATA[
{% if job.store is not empty %}APPLICATION_STORE="{{ job.store }}" {%endif%}{% if job.region is not empty %}SPRYKER_CURRENT_REGION="{{ job.region }}" {%endif%}COMMAND="{{ job.command | raw }}" bash /usr/bin/spryker.sh
]]>{% endblock command %}
