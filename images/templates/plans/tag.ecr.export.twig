{% macro tagApplication(application, separator) %}"${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${SPRYKER_PROJECT_NAME}-{{ application | lower }}:${TARGET_TAG}"{{ separator }}"${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${SPRYKER_PROJECT_NAME}-{{ application | lower }}:latest"{{ separator }}{% endmacro %}
{% macro tagService(service, separator) %}"${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${SPRYKER_PROJECT_NAME}-{{ service | lower }}:${TARGET_TAG}"{{ separator }}"${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${SPRYKER_PROJECT_NAME}-{{ service | lower }}:latest"{{ separator }}{% endmacro %}
