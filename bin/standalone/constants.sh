#!/bin/bash

# shellcheck disable=SC2034
TRUE=0
FALSE=1

SPRYKER_DOCKER_SDK_NAME="spryker_docker_sdk"
SPRYKER_DOCKER_SDK_NETWORK_NAME="${SPRYKER_DOCKER_SDK_NAME}"

GATEWAY_COMPOSE_FILE_NAME="traefik.compose.yml"
GATEWAY_CONFIG_FILE_NAME="traefik.yml"
GATEWAY_DIR_NAME="gateway"
GATEWAY_CONFIG_NAME="configs"
GATEWAY_CERT_DIR_NAME="certs"
GATEWAY_CONTEXT_PATH="${SPRYKER_DOCKER_SDK_DIR}/context/${GATEWAY_DIR_NAME}"
GATEWAY_CONTAINER_NAME="${SPRYKER_DOCKER_SDK_NAME}_gateway"

export SPRYKER_DOCKER_SDK_NETWORK_NAME
export GATEWAY_CONTAINER_NAME
